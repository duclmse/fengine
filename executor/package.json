{
  "name": "fengine",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "up": "npm run build && npm start",
    "start": "node dist/index.js",
    "build": "npx tsc -b --incremental --traceResolution",
    "pb": "parcel build ./src/index.js",
    "clean": "rm -rf ./dist",
    "cleanAll": "rm -rf ./node_modules && rm -rf ./dist && rm package-lock.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "duclmdev@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.6.3",
    "@types/google-protobuf": "^3.15.5",
    "@types/lodash": "^4.14.181",
    "async": "^3.2.3",
    "dotenv": "^16.0.0",
    "lodash": "^4.17.21",
    "redis": "^4.0.6",
    "vm2": "^3.9.9"
  },
  "devDependencies": {
    "@parcel/transformer-typescript-tsc": "^2.4.1",
    "grpc_tools_node_protoc_ts": "^5.3.2",
    "os-browserify": "^0.3.0",
    "parcel": "^2.4.1",
    "ts-loader": "^9.2.8",
    "typescript": "^4.6.3"
  }
}
