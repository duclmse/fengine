{
  "name": "fengine",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "build": "npx tsc -b --incremental --traceResolution",
    "clean": "rm -rf ./dist",
    "cleanAll": "rm -rf ./node_modules && rm -rf ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "duclmdev@gmail.com",
  "license": "ISC",
  "dependencies": {
    "@grpc/grpc-js": "^1.6.3",
    "@types/google-protobuf": "^3.15.5",
    "@types/lodash": "^4.14.181",
    "async": "^3.2.3",
    "dotenv": "^16.0.0",
    "lodash": "^4.17.21",
    "vm2": "^3.9.9"
  },
  "devDependencies": {
    "grpc-tools": "^1.11.2",
    "ts-loader": "^9.2.8",
    "typescript": "^4.6.3"
  }
}
